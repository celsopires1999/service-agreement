services:
    app:
        image: celsopires/service-agreement:v1.0.3
        ports:
            - "5000:5000"
        env_file:
            - .env          
        # restart: unless-stopped
        # command: tail -f /dev/null
